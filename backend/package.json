{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun --watch src/local.ts",
    "build": "bun build src/local.ts --outdir dist --target bun",
    "start": "bun src/local.ts",
    "migrate": "bun scripts/migrate.ts",
    "seed": "bun scripts/seed.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.11",
    "typescript": "^5.5.3"
  }
}
